// Duplicate authService.js (backup)
// Kept in cleanup_backup for recovery if needed.

// Duplicate authService stub (disabled).
// Use `./auth` (client/src/services/auth.js) as the canonical auth service.

export const disabledAuthService = true;

// Intentionally no default export to avoid duplicate `.default` declarations.
